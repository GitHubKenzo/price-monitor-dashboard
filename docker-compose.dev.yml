version: "3.9"

services:
  dev:
    build:
      context: .
      dockerfile: dev.Dockerfile
    container_name: price-monitor-dev
    volumes:
      - .:/app
      - ./tests:/app/tests
      - ./data:/data
    working_dir: /app
    command: pytest -v
